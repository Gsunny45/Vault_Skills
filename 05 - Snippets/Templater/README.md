# Templater Script Snippets
Store reusable Templater expressions and scripts here.

## Usage
Paste into any template file. Requires Templater plugin enabled
and template folder pointed to `03 - Templates/`.
